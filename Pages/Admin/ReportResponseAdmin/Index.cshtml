@page
@model JobForum3._0.Pages.Admin.ReportResponseAdmin.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Report[0].ResponseId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report[0].ReporterUserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report[0].ReporterName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report[0].ReportedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Report[0].IsHandled)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Report) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResponseId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReporterUserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReporterName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ReportedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsHandled)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
